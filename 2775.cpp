#include <stdio.h>
#include <stdlib.h>

int main() {
    int T;
    scanf("%d", &T);
    int strt[14];
    for (int i = 1; i <=14; i++)
        strt[i] = i;

    while(--T) {
        int k, n;
        scanf("%d %d", &k, &n);
        int *arr = (int *)malloc(sizeof(int)*n);
        
    }
}