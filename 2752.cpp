#include <cstdio>
#include <algorithm>
#include <vector>

using namespace std;
vector<int> vec(3);
int main() {
    scanf("%d %d %d", &vec);

    sort(arr[]);
    for (int i=0; i < 3; i++)
        printf("%d ", arr[i]);
    return 0;
}