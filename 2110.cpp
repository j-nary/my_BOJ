//BOJ_2110 공유기 설치 [골드 4]
//두번째 시도
//C는 변수다. 문제 제대로 읽기 !

#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

vector<long long> v;
int main() {
    ios::sync_with_stdio(false); cin.tie(nullptr);

    int N, C; cin >> N >> C;
    for (int i = 0; i < N; i++) {
        long long input; cin >> input;
        v.push_back(input);
    }

    sort(v.begin(), v.end());

    //거리
    int 

    cout << dist << '\n';
}